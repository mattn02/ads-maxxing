/** Execute the actual migration against isolated Postgres (PGlite), with minimal Supabase schema stubs. */
import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { pathToFileURL } from 'node:url';
import { randomUUID } from 'node:crypto';
const modulePath=process.env.PGLITE_MODULE;
if(!modulePath)throw new Error('Set PGLITE_MODULE to an isolated @electric-sql/pglite/dist/index.js installation. See docs/supabase-setup.md.');
const {PGlite}=await import(pathToFileURL(modulePath).href);
const db=new PGlite();
await db.exec(`create role anon;create role authenticated;create role service_role;create schema auth;create table auth.users(id uuid primary key);create function auth.uid() returns uuid language sql as $$select nullif(current_setting('request.jwt.claim.sub',true),'')::uuid$$;create schema storage;create table storage.buckets(id text primary key,name text,public boolean,file_size_limit bigint,allowed_mime_types text[]);create table storage.objects(bucket_id text,name text);create function storage.foldername(text) returns text[] language sql as $$select string_to_array($1,'/')$$;grant usage on schema public,auth to authenticated;`);
await db.exec(await readFile(new URL('../supabase/migrations/202609220001_workflow.sql',import.meta.url),'utf8'));
const owner=randomUUID(),other=randomUUID(),campaign=randomUUID();
await db.query('insert into auth.users values($1),($2)',[owner,other]);
const call=async(name,args)=> (await db.query(`select ${name}(${args.map((_,i)=>'$'+(i+1)).join(',')}) result`,args)).rows[0].result;
await call('create_campaign',[owner,campaign]);
await assert.rejects(call('claim_campaign',[other,campaign]),/unavailable/);
let lease=await call('claim_campaign',[owner,campaign]);
await assert.rejects(call('claim_campaign',[owner,campaign]),/busy/);
const research={id:randomUUID(),sources:[{url:'https://www.loopycases.com',title:'Loopy',description:'Cases',images:['https://cdn.example.com/product.png'],markdown:'Cases',fetchedAt:new Date().toISOString(),colors:{}}],colors:[],voice:'Friendly',audience:'People',sales:[],warnings:[]};
const session={id:campaign,messages:[],events:[],preferences:{},variants:[],research};
const save=async()=>{const result=await call('commit_campaign',[owner,campaign,lease.token,lease.revision,session,'loopycases.com',1]);lease.revision=result.revision;return result;};
const result=await save();
research.voice='Mutated';await assert.rejects(save(),/immutable/);research.voice='Friendly';
const source=randomUUID();
const asset={id:source,kind:'product_photo',storage_path:`${owner}/${result.brand_id}/${source}/image.png`,source_research_id:research.id,storage_state:'ready',content_hash:'hash',byte_size:100,mime_type:'image/png',width:576,height:1024,metadata:{}};
lease.revision=(await call('register_asset',[owner,campaign,lease.token,lease.revision,asset])).revision;
await assert.rejects(call('register_asset',[owner,campaign,lease.token,lease.revision,{...asset,storage_state:'pending'}]),/immutable/);
session.brief={id:randomUUID(),researchId:research.id,sourceAssetId:source,referenceImage:research.sources[0].images[0],productUrl:research.sources[0].url,headline:'A case',cta:'Shop',approvedAt:new Date().toISOString()};
await save();
session.brief.headline='Changed';await assert.rejects(save(),/immutable/);session.brief.headline='A case';
session.brief.backgroundCheckpoint={state:'attempted',attemptedAt:new Date().toISOString()};await save();
delete session.brief.backgroundCheckpoint;await assert.rejects(save(),/cannot be reset/);
session.brief.backgroundCheckpoint={state:'attempted',attemptedAt:(await db.query('select brief from ad_versions where id=$1',[session.brief.id])).rows[0].brief.backgroundCheckpoint.attemptedAt};
await db.query("update campaigns set lease_expires_at=now()-interval '1 minute' where id=$1",[campaign]);
const stale={...lease};lease=await call('claim_campaign',[owner,campaign]);
assert.equal((await db.query('select generation_state from ad_versions where id=$1',[session.brief.id])).rows[0].generation_state,'outcome_unknown');
await assert.rejects(call('commit_campaign',[owner,campaign,stale.token,stale.revision,session,'loopycases.com',1]),/lease expired/);
await call('release_campaign',[owner,campaign,stale.token]);
await assert.rejects(call('claim_campaign',[owner,campaign]),/busy/);
// Complete durable output and re-review/approval independently; current brief and historical variant share one row.
const bg=randomUUID(),scene=randomUUID();
for(const [id,kind] of [[bg,'generated_background'],[scene,'generated_scene'],[session.brief.id,'composed_ad']]) {
 lease.revision=(await call('register_asset',[owner,campaign,lease.token,lease.revision,{...asset,id,kind,storage_path:`${owner}/${result.brand_id}/${id}/image.png`,metadata:kind==='composed_ad'?{versionId:id}:{}}])).revision;
}
session.brief.backgroundCheckpoint={...session.brief.backgroundCheckpoint,state:'saved',asset:{id:bg}};
session.brief.sceneCheckpoint={state:'saved',asset:{id:scene}};
const variant={id:session.brief.id,imageUrl:`/api/outputs/${session.brief.id}`,brief:structuredClone(session.brief),research:structuredClone(research),status:'pending_review'};
session.variants=[variant];await save();
variant.status='reviewed';variant.review={verdict:'pass'};await save();
variant.status='approved';await save();
assert.equal((await db.query('select generation_state,review_status from ad_versions where id=$1',[variant.id])).rows[0].review_status,'approved');
const originalBrief=session.brief;
session.brief={...originalBrief,id:randomUUID(),parentVariantId:variant.id,backgroundCheckpoint:undefined,sceneCheckpoint:undefined,executionPlan:{background:{action:'reuse',assetId:bg},scene:{action:'reuse',assetId:scene}}};
await save();
const reuse=(await db.query('select background_asset_id,scene_asset_id from ad_versions where id=$1',[session.brief.id])).rows[0];assert.equal(reuse.background_asset_id,bg);assert.equal(reuse.scene_asset_id,scene);
delete session.brief;session.preferences={tone:'New direction'};session.research={...research,id:randomUUID()};await save();
assert.ok((await db.query('select approved_at from ad_versions where id=$1',[variant.id])).rows[0].approved_at);
// RLS and protected mutation privileges must work for the real authenticated role.
await db.query("select set_config('request.jwt.claim.sub',$1,false)",[other]);await db.exec('set role authenticated');
assert.equal((await db.query('select * from campaigns')).rows.length,0);
await assert.rejects(db.query("update campaigns set preferences='{}'"),/permission denied/);
await assert.rejects(call('claim_campaign',[owner,campaign]),/permission denied/);
await db.exec('reset role');await db.query("select set_config('request.jwt.claim.sub',$1,false)",[owner]);await db.exec('set role authenticated');
assert.equal((await db.query('select * from campaigns')).rows.length,1);
await db.exec('reset role');await db.close();
console.log('PASS: migration, immutable snapshots/content/assets, owner isolation/RLS, forbidden client writes/RPCs, atomic claims, stale writers, paid-attempt preservation. Hosted Storage/Auth remain separate live checks.');
